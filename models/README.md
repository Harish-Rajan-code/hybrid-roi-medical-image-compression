# Trained Models

Place the trained model checkpoints in this directory when running the complete pipeline.

Expected files:

- `unet_model.keras` — trained Attention U-Net for ROI segmentation
- `transformer_generator.pth` — trained Transformer-based ROI compression model

The trained model files are not included in this repository because of their size.
